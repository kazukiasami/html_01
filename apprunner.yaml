version: 1.0
runtime: nodejs18

build:
  commands:
    build:
      - npm install

run:
  runtime-version: 18
  command: npm start
  network:
    port: 8080

  secrets:
    - name: DB_SECRET
      value-from: arn:aws:secretsmanager:ap-northeast-1:620356661576:secret:rds!cluster-xxxx
