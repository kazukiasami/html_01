
# FastAPI on App Runner (Container / ECR)

This repository contains a FastAPI application
designed to run on AWS App Runner using
a container image pushed to Amazon ECR.

- Runtime configuration is defined in Dockerfile
- App Runner start command is intentionally empty
- Dockerfile is the single source of truth
